import path from 'path'
import fs from 'fs'

/**
class NativeFileService {

  constructor() {
    super()
  }

  list(userUUID, fsUUID, dpath, callback) {

    
    fs.readdir(dpath, (err, entries) => {
      if (err) return callback(err)
      return 
    })
  }  

  listAsync(userUUID, fsUUID, dpath) {
  }

  navListAsync(userUUID, dpath) {
  } 

  readFileAsync(userUUID, fpath) {
  
  }

  renameAsync(userUUID, fpath) {
  }
}
**/


