const child = require('child_process')
const fs = require('fs')
const os = require('os')

/**
 * 
 * @module Device
 * 
 */
class Device extends require('events') {
  constructor() {
    super()
  }

  cpuInfo() {

  }

  memInfo() {
    
  }
}