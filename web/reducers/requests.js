import request from 'superagent'

const storageUrl = '/storage'
const dockerUrl = '/docker'
const appstoreUrl = '/appstore'

const defaultState = {

  storage: null,
  docker: null,
  appstore: null
}

const reducers = (state = [] ) => {

}
